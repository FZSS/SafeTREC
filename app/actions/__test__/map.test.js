// import * as actions from '../map';

jest.mock('geofire');
jest.mock('firebase');

describe('Get concerns in region', () => {
  beforeEach(() => {
    jest.resetAllMocks();
  });

  it('gets all concerns', async () => {

  });
});
